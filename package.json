{
  "name": "describe-it-browser",
  "version": "1.0.3",
  "description": "",
  "source": "src/index.ts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "parcel ./demo/index.html",
    "build": "parcel build ./src/index.ts",
    "watch": "parcel watch ./src/index.ts",
    "types": "tsc -d --emitDeclarationOnly ./src/index.ts && mv src/*.d.ts dist/",
    "clean": "rm -fr dist/*",
    "test": "parcel build --target=node ./test/index.ts && node ./dist/index",
    "prepublish": "npm run clean && npm run build && npm run types"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/assert": "^1.5.6",
    "@types/dat.gui": "^0.7.7",
    "parcel-bundler": "^1.12.5",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "assert": "^2.0.0",
    "dat.gui": "^0.7.7"
  }
}
